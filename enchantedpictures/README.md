# Enchanted Pictures

Website-ready images live in:

- `showcase/` - optimized images used by `index.html` and `showcase.html`

Local-only source media lives in ignored folders:

- `showroom-pictures/` - original phone transfers
- `raw-iphone-imports/` - date-folder imports and large zip backups

Only `showcase/` is intended for GitHub Pages deploy.
